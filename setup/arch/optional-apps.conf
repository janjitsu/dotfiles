# Optional apps for the Arch/CachyOS setup — one per line, matching a
# script name (without .sh) in setup/arch/optional/. Comment out or
# delete a line to skip that app; add a new line + drop a matching
# script in optional/ to include more.
obs-studio
ardour
vlc
transmission
solaar
lact
