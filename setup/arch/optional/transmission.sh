#!/bin/bash
# transmission-gtk (not transmission-daemon) — a plain app you open/close,
# not a systemd service that stays resident seeding in the background
sudo pacman -S --noconfirm transmission-gtk
